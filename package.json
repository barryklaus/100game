{
  "name": "100game",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "tsc --noEmit && vite build",
    "build:cloudflare": "tsc --noEmit && tsc --noEmit -p tsconfig.worker.json && vite build --mode cloudflare",
    "test": "node scripts/test-rules.mjs && node scripts/test-online.mjs && node scripts/test-hosted.mjs && node scripts/test-presentation.mjs && node scripts/test-gyro.mjs",
    "preview": "vite preview --host 0.0.0.0",
    "dev:cloudflare": "wrangler dev --local",
    "deploy:cloudflare": "pnpm build:cloudflare && wrangler deploy"
  },
  "dependencies": {
    "peerjs": "^1.5.5",
    "three": "^0.180.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260917.1",
    "@types/three": "^0.180.0",
    "esbuild": "^0.28.2",
    "typescript": "^5.9.2",
    "vite": "^7.1.7",
    "wrangler": "^4.133.0"
  }
}
